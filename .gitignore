# ---- secrets ----
.env

# ---- python ----
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ---- macOS ----
.DS_Store

# ---- editor ----
.vscode/
.idea/
*.swp

# ---- archived manifests from earlier runs ----
*.archived

# ---- runtime logs (recreated by ingest_sp1500.py) ----
logs/

# ---- Claude Code local settings (keep launch.json, drop personal settings) ----
.claude/settings.local.json

# ---- data warehouse (lives on external SSD — see DATA_ROOT in .env) ----
# These directories were tracked until the SSD migration; they're now ignored
# so fresh clones don't accidentally repopulate them in the repo.
ASSET/
MACRO/
warehouse.duckdb
